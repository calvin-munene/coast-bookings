export default function HelpPage() { const faqs = [["When is my booking confirmed?","Instant bookings confirm after the payment provider callback is verified. Request-to-book reservations confirm after host acceptance and verified payment."],["How long does a host have to respond?","A request-to-book host has 12 hours. Once accepted, the guest has 2 hours to complete payment."],["Are group enquiries instant bookings?","No. Our team confirms requirements, sources properties and sends comparable quotation options."],["How are host payouts handled?","For the first release, all payouts require finance approval and are recorded manually. Eligibility begins 24 hours after check-in unless a dispute places the payout on hold."]]; return <><section className="page-hero"><div className="shell"><span className="section-kicker">Support centre</span><h1>Answers, without the runaround.</h1><p>Booking, payment and hosting guidance from a local operations team.</p></div></section><section className="section shell" style={{ maxWidth: 850 }}>{faqs.map(([q,a]) => <article key={q} style={{ padding: "22px 0", borderBottom: "1px solid var(--line)" }}><h2 style={{ color: "var(--navy)", fontSize: 20 }}>{q}</h2><p style={{ color: "var(--muted)", lineHeight: 1.7 }}>{a}</p></article>)}</section></>; }
