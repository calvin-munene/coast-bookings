// Supabase auth replaced with iron-session. This module is kept for import compatibility.
// Use src/lib/session.ts directly for new code.
export { getSessionUser as getServerUser } from "@/lib/session";
